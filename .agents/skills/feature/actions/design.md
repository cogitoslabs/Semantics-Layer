# Design Action

1. Go thru `context/current-feature.md`  
2. Ensure you have read thru all the documentation under `context` folder
3. Write the Detailed design specification in `context/features-specs/[feature-name].md`. Use the parsed Feature Name if provided; otherwise, derive `[feature-name]` based on the description provided in `current-feature.md`.
4. Set Status to "Design Completed" in `context/change-history.md`
5. Show the feature summary and key design decisions